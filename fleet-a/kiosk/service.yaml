apiVersion: v1
kind: Service
metadata:
  name: kiosk
  namespace: kiosk
  labels:
    app: kiosk
spec:
  selector:
    app: kiosk
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: 8080-tcp